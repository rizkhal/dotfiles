# trash
.zed
.DS_Store
zed

# nix
nix
direnv

# terminal
btop
htop
neofetch

# php
psysh
phpmon

# ── Dotfiles security ──
# Private keys — never include in dotfiles
.ssh/
!id_ed25519.pub
!known_hosts

# Machine-local overrides (each machine has its own)
.zshrc.local

# Secrets and credentials
.env*
.env.local
*.secret
*.token

# npm token file
.npmrc

# GPG keys (private)
.gpg/
*.gpg
*.asc

# Editor junk
.vscode/
.idea/
*.swp
*~

# Setup backups (auto-generated)
.dotfiles-backup/
